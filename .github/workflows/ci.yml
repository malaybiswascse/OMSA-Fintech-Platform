# This repository uses Jenkins + ArgoCD for CI/CD.
# GitHub Actions is intentionally disabled.
# See jenkins/Jenkinsfile for the full pipeline.
name: placeholder
on: [push]
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI/CD is handled by Jenkins and ArgoCD. See jenkins/Jenkinsfile."
